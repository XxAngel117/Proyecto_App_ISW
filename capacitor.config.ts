import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.isw',
  appName: 'App_UPSRJ',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
